/**
 * @template T
 * @param {T[]} lst
 * @returns {T}
 */
export const first = (lst) => lst[0]

/**
 * @template T
 * @param {T[]} lst
 * @returns {T}
 */
export const last = (lst) => lst[lst.length - 1]
